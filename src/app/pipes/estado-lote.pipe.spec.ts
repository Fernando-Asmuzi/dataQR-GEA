import { EstadoLotePipe } from './estado-lote.pipe';

describe('EstadoLotePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoLotePipe();
    expect(pipe).toBeTruthy();
  });
});
