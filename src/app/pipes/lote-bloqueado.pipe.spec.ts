import { LoteBloqueadoPipe } from './lote-bloqueado.pipe';

describe('LoteBloqueadoPipe', () => {
  it('create an instance', () => {
    const pipe = new LoteBloqueadoPipe();
    expect(pipe).toBeTruthy();
  });
});
